# pip install opencv-contrib-python (this specific one includes all open cv's functions as well as the comunities contributions)
# pip install caer (helpful functions so we can have a smoother workflow)